document.addEventListener("DOMContentLoaded", function () {
    // Esempio di codice eventuale
    console.log("Dashboard pronta!");

    // Se vuoi introdurre un tutorial con Intro.js:
    /*
    introJs().setOptions({
        steps: [
            { intro: "Benvenuto sulla Dashboard di Madre Natura!" },
            { element: document.querySelector(".service-card"), intro: "Clicca su un servizio per accedere." }
        ]
    }).start();
    */
});
